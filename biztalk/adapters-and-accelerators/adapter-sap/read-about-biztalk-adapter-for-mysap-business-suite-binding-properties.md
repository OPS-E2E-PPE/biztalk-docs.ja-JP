---
title: MySAP Business Suite のバインドのプロパティの BizTalk アダプターの説明を読む |Microsoft ドキュメント
ms.custom: ''
ms.date: 06/08/2017
ms.prod: biztalk-server
ms.reviewer: ''
ms.suite: ''
ms.tgt_pltfrm: ''
ms.topic: article
helpviewer_keywords:
- binding properties, setting
- binding properties
ms.assetid: 9cb83c1c-2f04-4f9e-8471-7f6db918da0d
caps.latest.revision: 20
author: MandiOhlinger
ms.author: mandia
manager: anneta
ms.openlocfilehash: ed1c9b05cda13063fd245bc4af147a661adf284c
ms.sourcegitcommit: 5abd0ed3f9e4858ffaaec5481bfa8878595e95f7
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 11/28/2017
ms.locfileid: "25967976"
---
# <a name="read-about-biztalk-adapter-for-mysap-business-suite-binding-properties"></a>MySAP Business Suite のバインドのプロパティの BizTalk アダプターの説明を読む
[!INCLUDE[adaptersap](../../includes/adaptersap-md.md)]を有効にすると、その実行時およびデザイン時の動作のいくつかを制御するいくつかのバインドのプロパティを表示します。 このセクションでは、これらのバインディング プロパティを説明し、設定する方法を説明するトピックへのリンクを示します。  
  
## <a name="the-sap-adapter-binding-properties"></a>SAP アダプターのバインドのプロパティ  
 次の表に、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]カテゴリにグループ化のプロパティをバインドします。 カテゴリは、各バインド プロパティが (SAPBinding) アダプタを構成する別のアプリケーションによって表示されるダイアログ ボックスに表示するノードを参照します。  
  
|プロパティのバインド|カテゴリ|Description|.NET 型|  
|----------------------|--------------|-----------------|---------------|  
|CloseTimeout|全般|指定します、[!INCLUDE[nextref_btsWinCommFoundation](../../includes/nextref-btswincommfoundation-md.md)]接続がタイムアウトを終了します。 既定値は 1 分です。|System.DateTime|  
|DataTypesBehavior|全般|SAP システムでは、DATS、TIM、および NUMC フィールドに対して指定するための正しい値は適用されません。 DATS、TIM の SAP データ ストアに無効な値があるとを使用して値を読み取るしよう NUMC フィールドと、クライアント プログラム、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]アダプターが例外をスローします。<br /><br /> また、SAP システムでは、同等の .NET 型がない DATS、TIM、および NUMC フィールドの最小値と最大値を表す特殊な値があります。 たとえば、DATS フィールドの最小値と最大値は 00000000 および 99999999、それぞれがないと同等の .NET DateTime 型のです。 さらに、DATS フィールドの最小値と最大値を DateTime.MinValue と DateTime.Max 値に変換するは、DATS フィールドの最小値または最大値と .NET の DateTime 型の最小値または最大値が同じでないためできません。<br /><br /> SAP システム内の特殊な値が発生したときに、アダプターの動作を制御するアダプターのクライアントを有効にするには設定、 **DataTypesBehavior**プロパティをバインドします。 これは、次のサブ プロパティを持つ複合バインディング プロパティです。<br /><br /> **DateTimeMaxToDats:** アダプター クライアントに送信される DateTime.MAX 値 DATS 値を送信するアダプターが従う必要がありますの動作を指定"9999-12-31T23:59:59.9999999"です。 これは、次の値を設定する可能性があります。<br /><br /> - **エラー**です。 これを設定すると、アダプターはエラーをスロー、クライアント プログラムが DateTime.MAX 値を送信する場合。<br /><br /> - **\<値\>** です。 これを設定すると、アダプター、指定した値を送信 SAP クライアント プログラムが DateTime.MAX 値を送信する場合。<br /><br /> 既定値は 99991231 です。<br /><br /> **DateTimeMaxToTims:** アダプター クライアントが値である DateTime.MAX を送信するときに、TIM 値を送信するアダプターが従う必要がありますの動作を指定"9999-12-31T23:59:59.9999999"です。 これは、次の値を設定する可能性があります。<br /><br /> - **エラー**です。 これを設定すると、アダプターはエラーをスロー、クライアント プログラムが DateTime.MAX 値を送信する場合。<br /><br /> -                              **\<値\>** です。 これを設定すると、アダプター、指定した値を送信 SAP クライアント プログラムが DateTime.MAX 値を送信する場合。<br /><br /> 既定値は 235959 です。<br /><br /> **DateTimeMinToDats:** アダプター クライアントに送信される DateTime.MIN 値 DATS 値を送信するアダプターが従う必要がありますの動作を指定"0001-01-01T00:00:00"です。 これは、次の値を設定する可能性があります。<br /><br /> -                              **エラー**です。 これを設定すると、アダプターはエラーをスロー、クライアント プログラムが DateTime.MIN 値を送信する場合。<br /><br /> -                              **\<値\>** です。 これを設定すると、アダプター、指定した値を送信 SAP クライアント プログラムが DateTime.MIN 値を送信する場合。<br /><br /> 既定値は 00010101 です。<br /><br /> **DateTimeMinToTims:** アダプター クライアントが値である DateTime.MIN を送信するときに、TIM 値を送信するアダプターが従う必要がありますの動作を指定"0001-01-01T00:00:00"です。 これは、次の値を設定する可能性があります。<br /><br /> - **エラー**です。 これを設定すると、アダプターはエラーをスロー、クライアント プログラムが DateTime.MIN 値を送信する場合。<br /><br /> - **\<値\>** です。 これを設定すると、アダプター、指定した値を送信 SAP クライアント プログラムが DateTime.MIN 値を送信する場合。<br /><br /> 既定値は 000000 です。<br /><br /> **DateTimeNullToDats:** DATS 値を送るアダプター クライアントが NULL の DateTime 値を送信するときに従う必要があります、アダプターの動作を指定します。 これは、次の値を設定する可能性があります。<br /><br /> - **エラー**です。 これを設定すると、アダプターはエラーをスロー、クライアント プログラムが NULL の DateTime 値を送信する場合。<br /><br /> - **SKIP**です。 これを設定すると、アダプター、フィールドのスキップは送信任意の値 sap クライアント プログラムが NULL の DateTime 値を送信する場合。<br /><br /> - **\<値\>** です。 これを設定すると、アダプター、指定した値を送信 SAP クライアント プログラムが NULL の DateTime 値を送信する場合。<br /><br /> 既定値は、スキップします。<br /><br /> **DateTimeNullToTims:** アダプター クライアントが NULL の DateTime 値を送信するときに、TIM 値を送信するアダプターが従う必要がありますの動作を指定します。 これは、次の値を設定する可能性があります。<br /><br /> - **エラー**です。 これを設定すると、アダプターはエラーをスロー、クライアント プログラムが NULL の DateTime 値を送信する場合。<br /><br /> - **SKIP**です。 これを設定すると、アダプター、フィールドのスキップは送信任意の値 sap クライアント プログラムが NULL の DateTime 値を送信する場合。<br /><br /> -                              **\<値\>** です。 これを設定すると、アダプター、指定した値を送信 SAP クライアント プログラムが NULL の DateTime 値を送信する場合。<br /><br /> 既定値は、スキップします。<br /><br /> **DatsMaxToDateTime:** アダプター、DATS を受信するときに、DateTime 値を取得するアダプターが従う必要がありますの動作を指定します。SAP からの 99999999 である最大値です。 これは、次の値を設定する可能性があります。<br /><br /> - **エラー**です。 これを設定すると、アダプターはエラーをスロー、DATS を受信した場合。SAP から最大値です。<br /><br /> - **NULL**です。 これを設定すると、アダプターは NULL を返します、DATS を受信した場合。SAP から最大値です。<br /><br /> - **\<値\>** です。 これを設定すると、アダプターは XSD:DateTime 形式で指定した値を解析し、クライアント プログラムを返します。<br /><br /> 既定値はエラーです。<br /><br /> **DatsMinToDateTime:** アダプター、DATS を受信するときに、DateTime 値を取得するアダプターが従う必要がありますの動作を指定します。最小値は 00000000、SAP からです。 これは、次の値を設定する可能性があります。<br /><br /> - **エラー**です。 これを設定すると、アダプターはエラーをスロー、DATS を受信した場合。SAP から最小値です。<br /><br /> - **NULL**です。 これを設定すると、アダプターは NULL を返します、DATS を受信した場合。SAP から最小値です。<br /><br /> - **\<値\>** です。 これを設定すると、アダプターは XSD:DateTime 形式で指定した値を解析し、クライアント プログラムを返します。<br /><br /> 既定値はエラーです。<br /><br /> **EmptyDatsToDateTime:** アダプター SAP から空の DATS 値を受信するときに、DateTime 値を取得するアダプターが従う必要がありますの動作を指定します。 これは、次の値を設定する可能性があります。<br /><br /> -                              **エラー**です。 これを設定すると、アダプターはエラーをスロー DATS 値が空 SAP から受信した場合。<br /><br /> - **NULL**です。 これを設定すると、アダプターは NULL を返します DATS 値が空 SAP から受信した場合。<br /><br /> - **\<値\>** です。 これを設定すると、アダプターは XSD:DateTime 形式で指定した値を解析し、クライアント プログラムを返します。<br /><br /> 既定値は 0001-01-01T00:00:00 です。<br /><br /> **EmptyNumcToInt:** SAP からアダプターは、空の NUMC 値 (すべてのスペース) を受け取るときに、整数値を取得するアダプターが従う必要がありますの動作を指定します。 これは、次の値を設定する可能性があります。<br /><br /> - **エラー**です。 これを設定すると、アダプターはエラーをスロー NUMC 値が空 SAP から受信した場合。<br /><br /> - **NULL**です。 これを設定すると、アダプターは NULL を返します NUMC 値が空 SAP から受信した場合。<br /><br /> - **\<値\>** です。 これを設定すると、アダプターでは、指定された値を有効な Int32 または Int64 値と、クライアント プログラムを返します。<br /><br /> 既定値は 0 です。<br /><br /> **EmptyTimsToDateTime:** アダプター SAP から空の TIM 値を受信するときに、DateTime 値を取得するアダプターが従う必要がありますの動作を指定します。 これは、次の値を設定する可能性があります。<br /><br /> -                              **エラー**です。 これを設定すると、アダプターはエラーをスロー TIM 値が空 SAP から受信した場合。<br /><br /> -                              **NULL**です。 これを設定すると、アダプターは NULL を返します TIM 値が空 SAP から受信した場合。<br /><br /> - **\<値\>** です。 これを設定すると、アダプターは XSD:DateTime 形式で指定した値を解析し、クライアント プログラムを返します。<br /><br /> 既定値は 0001-01-01T00:00:00 です。<br /><br /> **InvalidDatsToDateTime:** アダプターが SAP から無効な DATS 値を受信すると、DateTime 値を取得するアダプターが従う必要がありますの動作を指定します。 これは、次の値を設定する可能性があります。<br /><br /> -                              **エラー**です。 これを設定すると、アダプターはエラーをスロー SAP から無効な DATS 値を受信した場合。<br /><br /> -                              **NULL**です。 これを設定すると、アダプターは NULL を返しますに無効な DATS 値 SAP から受信した場合。<br /><br /> - **\<値\>** です。 これを設定すると、アダプターは XSD:DateTime 形式で指定した値を解析し、クライアント プログラムを返します。<br /><br /> 既定値はエラーです。<br /><br /> **InvalidNumcToInt:** アダプターは SAP から無効な NUMC 値を受け取ると、整数値を取得するアダプターが従う必要がありますの動作を指定します。 これは、次の値を設定する可能性があります。<br /><br /> - **エラー**です。 これを設定すると、アダプターはエラーをスロー SAP から無効な NUMC 値を受信した場合。<br /><br /> - **NULL**です。 これを設定すると、アダプターは NULL を返しますに無効な NUMC 値 SAP から受信した場合。<br /><br /> -                              **\<値\>** です。 これを設定すると、アダプターでは、指定された値を有効な Int32 または Int64 値と、クライアント プログラムを返します。<br /><br /> 既定値は 0 です。<br /><br /> **TimsMaxToDateTime:** アダプター、TIM を受信するときに、DateTime 値を取得するアダプターが従う必要がありますの動作を指定します。SAP から最大値です。 これは、次の値を設定する可能性があります。<br /><br /> -                              **エラー**です。 これを設定すると、アダプターはエラーをスロー、TIM を受信した場合。SAP から最大値です。<br /><br /> -                              **NULL**です。 これを設定すると、アダプターは NULL を返します、TIM を受信した場合。SAP から最大値です。<br /><br /> -                              **\<値\>** です。 これを設定すると、アダプターは XSD:DateTime 形式で指定した値を解析し、クライアント プログラムを返します。<br /><br /> 既定値はエラーです。|Microsoft.Adapters.SAP.SapDataTypesBehavior|  
|名前|全般|サポートされていません。|string|  
|OpenTimeout|全般|指定します、[!INCLUDE[nextref_btsWinCommFoundation](../../includes/nextref-btswincommfoundation-md.md)]接続がタイムアウトを開きます。 既定値は 1 分です。|System.DateTime|  
|ReceiveTimeout|全般|指定します、[!INCLUDE[nextref_btsWinCommFoundation](../../includes/nextref-btswincommfoundation-md.md)]メッセージの受信タイムアウトします。 基本的には、アダプターが受信メッセージを待機する時間の最大量このことを意味します。 既定値は、10 分です。<br /><br /> **重要:** の Idoc の受信など受信操作のことをお勧め、最大値をタイムアウトの設定; 24.20:31:23.6470000 (24 日) です。 付いたアダプターを使用するときに[!INCLUDE[btsBizTalkServerNoVersion](../../includes/btsbiztalkservernoversion-md.md)]タイムアウトを大きな値に設定した場合、アダプターの機能は影響しません。|System.DateTime|  
|SendTimeout|全般|指定します、[!INCLUDE[nextref_btsWinCommFoundation](../../includes/nextref-btswincommfoundation-md.md)]メッセージ送信のタイムアウト。 既定値は 1 分です。|System.DateTime|  
|EnableBizTalkCompatiblityMode|BizTalk|BizTalk 層チャネル バインド要素を読み込む必要があるかどうかを指定します。 BizTalk のトランザクションを経由で配信を有効にする、BizTalk 層チャネル バインド要素が読み込まれて、 [!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)] SAP システムにします。<br /><br /> これを設定して**true**をバインド要素を読み込めません。 それ以外の場合、これを設定して**false**です。<br /><br /> アダプターを使用するときに[!INCLUDE[btsBizTalkServerNoVersion](../../includes/btsbiztalkservernoversion-md.md)]、常にプロパティに設定する必要があります**true**です。 アダプターを使用するときに[!INCLUDE[btsVStudioNoVersion](../../includes/btsvstudionoversion-md.md)]、常にプロパティに設定する必要があります**false**です。|bool (System.Boolean)|  
|EnableBusinessObjects|Bapi|このプロパティは推奨されません。 アダプターは常に表示される、 **BAPI**ノードを使用してメタデータを参照するときに、[!INCLUDE[addadapterservrefshort](../../includes/addadapterservrefshort-md.md)]または[!INCLUDE[consumeadapterservshort](../../includes/consumeadapterservshort-md.md)]です。 設定と同じ動作が**EnableBusinessObjects**に**true**で[!INCLUDE[adapterpackversion](../../includes/adapterpackversion-md.md)]1.0 です。 |bool (System.Boolean)|  
|EnableConnectionPooling|接続|指定するかどうか、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]接続プールが有効にします。 既定値は**true**、接続プールが有効になっていることを指定します。|bool (System.Boolean)|  
|IdleConnectionTimeout|接続|指定します、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]アイドル接続のタイムアウト。 プール内の接続がアイドル状態 (未使用) をこのタイムアウトを超える期間、接続が破棄されます。 既定では 15 分です。 アイドル接続のタイムアウトは、使用されていない、プール内の接続にのみ適用されます。 データ待っているアクティブな (開いている) 接続には影響しません。|System.DateTime|  
|MaxConnectionsPerSystem|接続|内の接続の最大数を指定、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]接続プールします。 既定値は 50 です。 **MaxConnectionsPerSystem**アプリケーション ドメイン内で静的なプロパティです。 つまり、変更すると、 **MaxConnectionsPerSystem**アプリケーション ドメインで 1 つのバインディング インスタンスの場合は、新しい値がそのアプリケーション ドメイン内のすべてのバインディング インスタンスから作成されたすべてのオブジェクトに適用されます。<br /><br /> **重要:** SAP クライアント ライブラリ (librfc32u.dll) を既定では、SAP システムへの 100 個の接続の最大値をサポートしています。 この接続の数を超えた場合、例外がスローされます、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]です。 このため、設定を適用しないで**MaxConnectionsPerSystem** SAP クライアント ライブラリによってサポートされる接続の数より大きい値にします。 CPIC_MAX_CONV、環境変数を設定して、SAP クライアント ライブラリをサポートする接続の数を増やすことができます。 変更を反映するためには、この変数を設定した後、コンピューターを再起動する必要があります。|int 型 (System.Int32)|  
|RfcAllowStartProgram|接続|RFC パートナーによって必要な場合は、RFC クライアント ライブラリを開始できること、外部プログラムを指定します。 たとえば、内部的には、アダプターのクライアントを実行しているコンピューター上のプログラムを起動する RFC を呼び出す場合、そのプログラムのこのプロパティの名前のバインドを指定する必要があります。<br /><br /> このバインディングのプロパティの複数のプログラムを指定する場合は、セミコロンで区切る必要があります。 たとえばを指定する場合、`sapftp`と`saphttp`のプログラム、する必要がありますを指定すると`sapftp;saphttp`です。<br /><br /> また、次の条件が満たされてを確認します。<br /><br /> RFC で必要な-外部プログラムは、アダプターのクライアントを実行しているコンピューターで使用できます。<br /><br /> -外部プログラムの場所は、アダプターのクライアントを実行するコンピューター上のパス変数内に存在します。<br /><br /> たとえば、BAPI_DOCUMENT_CHECKOUTVIEW2 で内部的には、プログラムが実行`sapftp`です。 そのため、この RFC を呼び出し中に設定する必要あります、 **RfcAllowStartProgram**にプロパティのバインド`sapftp`です。 またことを確認、`sapftp`プログラムがローカルで利用可能の場所と、`sapftp`プログラムは、アダプターのクライアントを実行しているコンピューター上のパス変数に追加します。|文字列|  
| ConnectorType | ConnectorType | クラシック RFC を使用して SAP に接続するか、for .NET (NCo)、SAP コネクタを使用します。 | |   
|EnablePerformanceCounters|診断|有効にするかどうかを指定します、[!INCLUDE[afproductnameshort](../../includes/afproductnameshort-md.md)]パフォーマンス カウンターと[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]LOB の待機時間のパフォーマンス カウンター。 既定値は**false**; パフォーマンス カウンターが無効です。 LOB の待機時間のパフォーマンス カウンターを測定するのにかかった合計時間、 [!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)] SAP システムへの呼び出しを行うのです。<br /><br /> **注: EnablePerformanceCounters**アプリケーション ドメイン (appdomain) 内の静的プロパティは、[!INCLUDE[afproductnameshort](../../includes/afproductnameshort-md.md)]パフォーマンス カウンターが、アダプターの LOB の待機時間のパフォーマンス カウンターのインスタンス プロパティです。 つまり、その変更**EnablePerformanceCounters**アプリ ドメインでバインド インスタンスがします。<br /><br /> -有効または無効にする、[!INCLUDE[afproductnameshort](../../includes/afproductnameshort-md.md)]同じアプリ ドメイン内のすべてのバインディング インスタンスから作成されたすべてのオブジェクトのパフォーマンス カウンター。<br /><br /> -有効にするにまたは、変更を行った後、そのバインディングのインスタンスから作成されたオブジェクトに対してのみ、アダプターの LOB の待機時間のパフォーマンス カウンターを無効にします。|bool (System.Boolean)|  
|AutoConfirmSentIdocs|Idoc|指定するかどうか、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]自動コミット tRFC クライアント呼び出しが Idoc を送信するために使用します。 既定値は**false**; 自動コミットが無効になっています。 自動コミットが無効になっている場合、クライアント アプリケーションする必要があります明示的にコミット tRFC の呼び出しを呼び出すことによって、 **RfcConfirmTransID**操作します。 **RfcConfirmTransID**操作が側に表示される特別な操作、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]です。 下に表示されます、TRFC ノードを使用するときに、[!INCLUDE[addadapterservreflong](../../includes/addadapterservreflong-md.md)]または[!INCLUDE[consumeadapterservlong](../../includes/consumeadapterservlong-md.md)]です。|bool (System.Boolean)|  
|padReceivedIdocWithSpaces|Idoc|ReceiveIdoc 操作によって返される各行が適切な長さにスペースが埋め込まれるかどうかを指定します。 既定値は false です。行が埋め込まれていません。|bool (System.Boolean)|  
|EnableSafeTyping|メタデータ|有効またはセーフである入力を無効にします。 既定値は**false**以外の場合は安全な入力が無効になっています。 この機能は、アダプターが特定の SAP データ型を表示する方法を制御します。 安全な入力の詳細については、次を参照してください。 [SAP の基本データ型](../../adapters-and-accelerators/adapter-sap/basic-sap-data-types.md)です。|bool (System.Boolean)|  
|flatFileSegmentIndicator|メタデータ|指定するかどうか、 \<appinfo\>タグは、セグメントの種類を含める必要がありますまたはセグメントの定義の解析をフラット ファイル Idoc です。 XML スキーマの要素、ただし、常に格納するようにセグメントの定義名のみに注意してください。 2 つの値がある、 **FlatFileSegmentIndicator**プロパティ。<br /><br /> - **SegmentDefinition**フラット ファイルが IDoc 内の各セグメントのセグメントの定義を含めることを示します。<br /><br /> -                      **SegmentType**フラット ファイルが IDoc 内の各セグメントのセグメントの種類を含めることを示します。<br /><br /> 既定値は**SegmentDefinition**です。|enum Microsoft.Adapters.SAP.FlatFileSegmentIndicator|  
|GenerateFlatfileCompatibleIdocSchema|メタデータ|指定するかどうかフラット ファイル\<appinfo\> IDoc メッセージ スキーマにタグを追加する必要があります。 BizTalk フラット ファイル パーサーが必要です。 既定値は**true**、ことを指定\<appinfo\>タグはスキーマに追加されます。|bool (System.Boolean)|  
|receiveIDocFormat|メタデータ|ディスパッチされたメッセージの XML 形式を指定します、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]受信側 (SAP アダプターから) で、クライアント アプリケーションにします。 3 つの値がある、 **ReceiveIDocFormat**プロパティ。<br /><br /> - **文字列**IDoc メッセージを 1 つの文字列フィールドとして表現する必要がありますを指定します、[!INCLUDE[nextref_btsWinCommFoundation](../../includes/nextref-btswincommfoundation-md.md)]メッセージ。<br /><br /> - **型指定された**IDoc メッセージを解析し、厳密に型指定されたとして表されることを指定[!INCLUDE[nextref_btsWinCommFoundation](../../includes/nextref-btswincommfoundation-md.md)]メッセージ。<br /><br /> -                      **Rfc**ことを指定、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]として着信 RFC 呼び出しを渡す必要があります、 [!INCLUDE[nextref_btsWinCommFoundation](../../includes/nextref-btswincommfoundation-md.md)] RFC パラメーターを持つメッセージ。<br /><br /> 既定値は**型指定された**です。|enum Microsoft.Adapters.SAP.IdocReceiveFormat|  
|SncLibrary|SNC|コンピューターに SNC ライブラリの場所を指定します。 のみ; ライブラリのファイル名を入力する必要がある場合は、PATH 環境変数には、ライブラリが存在するディレクトリが含まれています、それ以外の場合、完全なパスを指定する必要があります。 **SncLibrary**バインディング プロパティは、SAP 接続プロパティを表示します。 詳細については、SAP のマニュアルを参照してください。<br /><br /> 接続をセキュリティで保護されたネットワーク通信 (SNC) を有効にする URI で UseSnc パラメーターを設定する必要があります。 SAP 接続 URI の詳細については、次を参照してください。 [SAP システム接続 URI を作成する](../../adapters-and-accelerators/adapter-sap/create-the-sap-system-connection-uri.md)です。|string|  
|SncPartnerName|SNC|SNC のパートナー名を指定します。 **SncPartnerName**バインディング プロパティは、SAP 接続プロパティを表示します。 詳細については、SAP のマニュアルを参照してください。<br /><br /> 接続をセキュリティで保護されたネットワーク通信 (SNC) を有効にする URI で UseSnc パラメーターを設定する必要があります。 SAP 接続 URI の詳細については、次を参照してください。 [SAP システム接続 URI を作成する](../../adapters-and-accelerators/adapter-sap/create-the-sap-system-connection-uri.md)です。|string|  
|TidDatabaseConnectionString|TrfcServer|データベース接続文字列を指定して、SQL Server データベースの[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]を使用してトランザクション Id (Tid) を格納します。 [!INCLUDE[adapterpacknoversion](../../includes/adapterpacknoversion-md.md)]セットアップ ウィザードは、アダプターによって受信トランザクション (tRFC) の RFC サーバー呼び出しを有効にする Tid を格納するために使用する SQL Server オブジェクトを作成、既存のデータベースに対して SQL Server の管理者によって実行する必要がありますをいくつかの SQL スクリプトをインストールします。 SQL スクリプトの詳細についてを参照してください、[!INCLUDE[adapterpacknoversion](../../includes/adapterpacknoversion-md.md)]インストール ガイドで使用可能な*\<インストール ドライブ\>*: \Program Files\Microsoft [!INCLUDE[adapterpacknoversion](../../includes/adapterpacknoversion-md.md)]\Documents です。<br /><br /> SAP からの Idoc のまたは Rfc を受信するための受信 tRFC サーバー呼び出しを有効にするには、このプロパティを設定する必要があります。 既定値は**null**; tRFC サーバー呼び出しが有効ではありません。<br /><br /> 次の形式では、接続文字列を指定できます。<br /><br /> `Data Source=<myServerAddress>;Initial Catalog=<myDataBase>;User Id=<myUsername>;Password=<myPassword>;`<br /><br /> 接続文字列を指定するには、省略記号ボタンをクリックして **([...])** バインドのプロパティに対して必要な接続文字列プロパティの値を入力します。|string|  
|AcceptCredentialsInUri|表示されません、[!INCLUDE[consumeadapterservshort](../../includes/consumeadapterservshort-md.md)]または[!INCLUDE[addadapterservrefshort](../../includes/addadapterservrefshort-md.md)]です。|SAP 接続 URI が、SAP システムのユーザーの資格情報を含めることができるかどうかを指定します。 既定値は**false**、接続 URI でユーザー資格情報を無効にします。 場合**AcceptCredentialsInUri**は**false** 、SAP 接続 URI には、ユーザーの資格情報が含まれています、[!INCLUDE[adaptersap_short](../../includes/adaptersap-short-md.md)]例外をスローします。 設定することができます**AcceptCredentialsInUri**に**true**場合は、URI で資格情報を指定する必要があります。 詳細については、次を参照してください。 [SAP システム接続 URI を作成する](../../adapters-and-accelerators/adapter-sap/create-the-sap-system-connection-uri.md)です。|bool (System.Boolean)|  
  
## <a name="how-do-i-set-sap-binding-properties"></a>SAP バインドのプロパティを設定する方法は?  
 SAP システムへの接続を指定する場合は、SAP バインドのプロパティを設定できます。 バインドのプロパティを設定する方法についてはときにします。  
  
-   使用して、[!INCLUDE[consumeadapterservlong](../../includes/consumeadapterservlong-md.md)]または、[!INCLUDE[addadapterservreflong](../../includes/addadapterservreflong-md.md)]を参照してください[Visual Studio での SAP システムへの接続](../../adapters-and-accelerators/adapter-sap/connect-to-the-sap-system-in-visual-studio.md)です。  
  
    > [!IMPORTANT]
    >  使用しているときに、[!INCLUDE[consumeadapterservshort](../../includes/consumeadapterservshort-md.md)]または[!INCLUDE[addadapterservrefshort](../../includes/addadapterservrefshort-md.md)]文字列型のバインド プロパティの値を指定しないと、そのバインドのプロパティは、バインド ファイル (XML ファイル) または app.config ファイルでは使用できませんし、既定値が null の場合は、それぞれします。 必要があります手動で追加するバインディングのプロパティとその値をバインド ファイルまたは app.config ファイルに必要な場合です。  
  
-   送信ポートを構成するか、受信ポート (場所) で、[!INCLUDE[btsBizTalkServerNoVersion](../../includes/btsbiztalkservernoversion-md.md)]ソリューションを参照してください[SAP アダプターを物理ポートのバインドを手動で構成](../../adapters-and-accelerators/adapter-sap/manually-configure-a-physical-port-binding-to-the-sap-adapter.md)です。  
  
-   プログラミング ソリューションで、WCF チャネル モデルを使用して、参照してください[SAP を使用して、チャネルの作成](../../adapters-and-accelerators/adapter-sap/create-a-channel-using-sap.md)です。  
  
-   プログラミング ソリューションで WCF サービス モデルを使用して、参照してください[クライアントを SAP システムのバインド構成](../../adapters-and-accelerators/adapter-sap/configure-a-client-binding-for-the-sap-system.md)です。  
  
-   使用する WCF ServiceModel メタデータ ユーティリティ ツール (svcutil.exe) を参照してください[BizTalk adapter 用 mySAP Business Suite、ServiceModel メタデータ ユーティリティ ツールを使用して](../../adapters-and-accelerators/adapter-sap/use-the-servicemodel-metadata-utility-with-the-sap-adapter-in-biztalk.md)です。  
  
## <a name="see-also"></a>参照  
[SAP アプリケーションを開発する](../../adapters-and-accelerators/adapter-sap/develop-your-sap-applications.md)